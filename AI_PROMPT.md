Create a complete professional business website in React JS using a true multi-page architecture, not a React Router SPA. Use Vite with multiple HTML entry files for Home, About, Services, Portfolio, and Contact pages. The website should include modern responsive design, smooth CSS animations, scroll reveal effects, a working testimonial carousel, a mobile navigation menu, reusable React components, professional business content, service cards, portfolio/case-study cards, a contact form UI, and random business-related images selected from a curated image list on page load. Use clean folder structure, semantic HTML, accessible buttons and alt text, modern CSS variables, and production-ready code. Provide all source files including package.json, vite.config.js, page-specific index.html files, React entry files, shared components, data files, CSS, README, and setup instructions. The final output should be complete enough to install with npm install, run with npm run dev, and build with npm run build.
